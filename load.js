const ver = "v2.5r";

fetch('https://cdn.jsdelivr.net/gh/omgbroops/mp3player@' + ver + '/index.html').then(response => response.text()).then(html => { const container = document.querySelector('body'); container.innerHTML = '<!DOCTYPE html>' + '<html>' + '<head>' + '<title>New Tab</title>' + '</head>' + '<body>' + '<script>loadSessionDB();</script>' + html + '</body>' + '</html>'; container.querySelectorAll('script').forEach(oldScript => { const newScript = document.createElement('script'); if (oldScript.src) { newScript.src = oldScript.src; newScript.async = false; } else { newScript.textContent = oldScript.textContent; } oldScript.parentNode.replaceChild(newScript, oldScript); }); });
